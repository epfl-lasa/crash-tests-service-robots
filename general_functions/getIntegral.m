
function int=getIntegral(intC,input,tDelta)
    % This function calculates the numerical integral of a data series with a
    % given freceuncy
    % get1int(input,frec,intC)    
    
    int = ((intC + input)/2 ) * (tDelta) ;
    
end